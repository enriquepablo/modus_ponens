fact        = { value ~ pred ~ value }

var         = @{ "<" ~ "__"? ~ "X" ~ ('0'..'9')+ ~ ">" }

pred        = @{ "ISA" ~ ('0'..'9')+ | "IS" ~ ('0'..'9')+ }

v_name      = @{ ASCII_ALPHANUMERIC+ }

value       = _{ v_name | var }

WHITESPACE  = { (" " | "\t" | NEWLINE)+ }
